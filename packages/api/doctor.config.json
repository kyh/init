{
  "$schema": "https://react.doctor/schema/config.json",
  "ignore": {
    "overrides": [
      {
        // Deliberate non-secret Stripe test placeholder. Network calls still fail closed.
        "files": ["src/auth/auth.ts"],
        "rules": ["react-doctor/secret-in-fallback"]
      }
    ]
  }
}
